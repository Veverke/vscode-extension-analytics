# Release
#
# Purpose: Produces a versioned GitHub Release for the analytics dashboard.
# Triggered by pushing a semver tag (e.g. v1.2.3) or manually via
# workflow_dispatch. Validates the full test suite, verifies that
# package.json version matches the tag, builds the static site, creates a
# GitHub Release with auto-extracted CHANGELOG notes, and attaches the dist/
# bundle as a downloadable artifact. GitHub Pages deployment itself is handled
# by deploy.yml and is NOT part of this workflow.
name: Release

# Triggered by pushing a version tag: git tag v1.2.3 && git push --tags
# Or manually via workflow_dispatch (e.g. for pre-releases / dry-runs).
#
# This is a web app deployed to GitHub Pages — there is no .vsix package or
# marketplace publishing step. A release creates a tagged GitHub Release with
# auto-extracted CHANGELOG notes and uploads the static build as an artifact.
#
# The version in package.json MUST match the pushed tag before tagging.
# Convention: bump version in package.json, commit, tag, push tag.

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"        # stable:     v1.2.3
      - "v[0-9]+.[0-9]+.[0-9]+-*"      # pre-release: v1.2.3-beta.1

  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to release (must already exist, e.g. v1.2.3)"
        required: true
      dry_run:
        description: "Dry run — package but do not publish"
        type: boolean
        default: false

# Derived values used across jobs
env:
  TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}

jobs:
  # ─── Job 1: Validate ────────────────────────────────────────────────────────
  # Full test suite must be green before any artifact is produced.
  validate:
    name: Validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.TAG }}

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      # All quality gates must be green before any artifact is produced
      - name: Lint
        run: npm run lint

      - name: TypeScript type check
        run: npx tsc --noEmit

      - name: Unit tests with coverage
        run: npm run test -- --coverage

      # Non-blocking — coverage badge/history only
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5.5.4
        if: always()
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage/lcov.info
          flags: unit,release
          name: release-${{ env.TAG }}
          fail_ci_if_error: false
  
      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      # First build is used for E2E testing
      - name: Build
        run: npm run build

      - name: E2E tests
        run: npm run test:e2e

      # Prevents a mismatch between the git tag and the published package version
      - name: Verify package.json version matches tag
        run: |
          PKG_VERSION="v$(node -p "require('./package.json').version")"
          if [ "$PKG_VERSION" != "${{ env.TAG }}" ]; then
            echo "::error::package.json version ($PKG_VERSION) does not match tag (${{ env.TAG }})"
            exit 1
          fi
          echo "Version check passed: $PKG_VERSION"

      - name: Build static site
        run: npm run build

      # Passed to the github-release job so the dist/ folder doesn’t need rebuilding
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
          retention-days: 30

  # ─── Job 2: GitHub Release ───────────────────────────────────────────────────
  # Creates a GitHub Release tagged at the semver version. Extracts release
  # notes from CHANGELOG.md (falls back to git log if no matching section is
  # found) and marks the release as a pre-release when the tag contains a
  # pre-release suffix (e.g. -beta.1). The static build from the validate job
  # is attached as a downloadable artifact. GitHub Pages deployment is handled
  # separately by deploy.yml on push to main.
  github-release:
    name: GitHub Release
    needs: validate
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.TAG }}
          fetch-depth: 0

      # Reuse the dist/ built and validated in the validate job
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      # Tries to pull the matching section from CHANGELOG.md; falls back to git log
      - name: Extract changelog for this version
        id: changelog
        run: |
          # Extract the block between this tag's header and the previous one in CHANGELOG.md.
          # Falls back to git log if CHANGELOG.md is absent or has no matching section.
          TAG="${{ env.TAG }}"
          if [ -f CHANGELOG.md ]; then
            NOTES=$(awk "/^## \[?${TAG#v}\]?/{flag=1; next} flag && /^## /{flag=0} flag{print}" CHANGELOG.md)
          fi
          if [ -z "$NOTES" ]; then
            PREV_TAG=$(git describe --tags --abbrev=0 "${TAG}^" 2>/dev/null || echo "")
            if [ -n "$PREV_TAG" ]; then
              NOTES=$(git log --pretty=format:"- %s (%h)" "${PREV_TAG}..${TAG}")
            else
              NOTES=$(git log --pretty=format:"- %s (%h)" "${TAG}")
            fi
          fi
          # Write to a file to safely pass multi-line content
          echo "$NOTES" > release-notes.txt
          echo "notes_file=release-notes.txt" >> "$GITHUB_OUTPUT"

      # Tags containing a hyphen (e.g. v1.0.0-beta.1) are marked as pre-releases
      - name: Determine pre-release flag
        id: prerelease
        run: |
          if echo "${{ env.TAG }}" | grep -qE '\-'; then
            echo "is_prerelease=true" >> "$GITHUB_OUTPUT"
          else
            echo "is_prerelease=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.TAG }}
          name: ${{ env.TAG }}
          body_path: ${{ steps.changelog.outputs.notes_file }}
          prerelease: ${{ steps.prerelease.outputs.is_prerelease }}
